# دليل البدء السريع

## ✅ الخطوات المطلوبة

### 1️⃣ تشغيل SQL Scripts في Supabase

انتقل إلى **Supabase SQL Editor** وقم بتشغيل هذه الملفات بالترتيب:

```bash
scripts/001_create_projects_table.sql
scripts/002_create_users_table.sql
scripts/003_create_project_members.sql
scripts/004_create_whatsapp_tables.sql
scripts/005_create_messages_table.sql
scripts/006_create_contacts_table.sql
scripts/007_create_message_templates.sql
scripts/008_create_analytics_table.sql
scripts/009_seed_trusted_users.sql
```

⚠️ **أهم:**
- قم بتشغيل الـ scripts **بنفس الترتيب**
- انتظر انتهاء كل script قبل تشغيل التالي

### 2️⃣ التحقق من الحسابات

بعد تشغيل script 009، تأكد من إضافة الحسابات الثلاثة:

```sql
SELECT * FROM public.trusted_users;
```

يجب أن ترى 3 صفوف:
- admin@alazab.com
- mohamed@alazab.com
- ceo@alazab.com

### 3️⃣ اختبار الدخول

انتقل إلى `http://localhost:3000/auth/trusted-login`

أدخل أحد الحسابات الثلاثة:
- **البريد:** admin@alazab.com
- **كلمة المرور:** لا تحتاج (دخول مباشر)

### 4️⃣ اختبر المنصة

بعد الدخول يجب أن تصل إلى الصفحة الرئيسية مباشرة.

## 🚀 النشر على Vercel

1. أضف جميع متغيرات البيئة من `.env.example`
2. اضغط **Publish** في v0
3. سيتم نشر المشروع على Vercel تلقائياً

## 📚 المسارات الرئيسية

| المسار | الوصف |
|-------|--------|
| `/auth/trusted-login` | صفحة الدخول المباشر |
| `/` | الصفحة الرئيسية (محمية) |
| `/dashboard/[projectSlug]` | لوحة تحكم المشروع |
| `/auth/logout` | تسجيل الخروج |
| `/api/auth/me` | الحصول على بيانات المستخدم الحالي |

## 🔐 الأمان

- جميع المسارات محمية بـ Session-based authentication
- الجلسات تنتهي بعد سنة واحدة
- Cookies آمنة وحساسة (HttpOnly)
- RLS policies على جميع الجداول

## 🐛 استكشاف الأخطاء

إذا واجهت مشكلة:

1. **تحقق من الحسابات في قاعدة البيانات:**
   ```sql
   SELECT * FROM public.trusted_users WHERE email = 'admin@alazab.com';
   ```

2. **تحقق من جدول الجلسات:**
   ```sql
   SELECT * FROM public.user_sessions;
   ```

3. **شاهد console logs في المتصفح** (F12)

4. **تأكد من البريد الصحيح:** استخدم lowercase فقط

## ✨ ما التالي؟

- أضف مشاريع WhatsApp
- أنشئ قوالب للرسائل
- ابدأ بإرسال الرسائل عبر API

**السرعة في الإنتاج! 🚀**
