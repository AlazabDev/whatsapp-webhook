# قائمة فحص الإعداد

## ✅ قبل النشر

- [ ] قرأت `QUICK_START.md`
- [ ] شغلت جميع SQL scripts بالترتيب في Supabase
- [ ] تحققت من وجود الحسابات الثلاثة في جدول `trusted_users`
- [ ] اختبرت الدخول مع أحد الحسابات
- [ ] تحققت من الصفحة الرئيسية تفتح بدون أخطاء

## 📝 البيانات المطلوبة

تأكد من وجود هذه المتغيرات في **Vercel Vars**:

```
NEXT_PUBLIC_SUPABASE_URL = your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY = your_anon_key
SUPABASE_SERVICE_ROLE_KEY = your_service_role_key
NEXT_PUBLIC_APP_URL = your_app_url
SESSION_SECRET = your_secret_key
WHATSAPP_ACCESS_TOKEN = your_token (اختياري حالياً)
WHATSAPP_WEBHOOK_VERIFY_TOKEN = your_token (اختياري)
```

## 🔍 المسارات المهمة

| الملف | الوصف |
|------|--------|
| `app/auth/trusted-login/page.tsx` | صفحة الدخول |
| `app/api/auth/trusted-login/route.ts` | API الدخول |
| `lib/auth/trusted-auth.ts` | منطق المصادقة |
| `proxy.ts` | حماية المسارات |
| `app/page.tsx` | الصفحة الرئيسية |
| `scripts/009_seed_trusted_users.sql` | إدراج الحسابات |

## 🚀 النشر

```bash
# 1. اختبر محلياً
npm run dev

# 2. انشر على Vercel
# اضغط "Publish" في v0

# 3. تحقق من الـ logs في Vercel Dashboard
```

## 📞 المساعدة

إذا حصل خطأ:

1. افتح Developer Tools (F12)
2. انظر إلى Network tab
3. افحص الـ request/response
4. شاهد console logs

## ✨ النتيجة النهائية

بعد الانتهاء ستحصل على:
- ✅ نظام دخول آمن بدون كلمة مرور
- ✅ جلسات تستمر لمدة سنة
- ✅ لوحة تحكم منصة WhatsApp
- ✅ نظام إدارة المشاريع
- ✅ API جاهز للتكامل

**جاهز للإنتاج! 🎉**
